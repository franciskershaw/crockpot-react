# CFE-003 — Landing page + palette + fonts

Grilled 2026-08-28. Not implemented yet.

**Implementation mode: Claude-implemented, founder-supervised.** Claude
writes the code against this doc and the 5 landing PNGs; founder reviews
at commit boundaries. No unit tests for the page (see Verification).

Pulled into Round 1 so the log-in milestone lands on a finished public
entry point rather than the throwaway `SignInPlaceholder`. Also the
ticket where the **colour palette** and the **font system** land — both
outlive the page and are consumed by every screen from CFE-004 on, so
they get real detail here and a summary in the master spec.

## Picking this up cold

1. Read this whole doc, then `docs/handoffs/CFE-001.md`'s "Deltas from
   the plan" (the `lib/http` naming, `sonner` minus `next-themes`, the
   oxlint carve-out for `src/components/ui/**`, `*.md` Prettier-ignored).
2. Look at all 5 PNGs in `../screenshots/landing page/`:
   `lp1.0.png` (desktop hero + "Three steps"), `lp1.1.png` (desktop
   planner tease + "Pick a plan" + footer), `lp2.0.png` (mobile hero),
   `lp2.1.png` (mobile planner tease + pricing + footer),
   `loggedOutUserNav.png` (mobile bottom nav).
3. Read `src/index.css` (token names to remap), `src/app/AppRoutes.tsx`,
   `src/features/auth/SignInPlaceholder.tsx` (deleted by this ticket),
   `src/components/ui/button.tsx`.
4. `git log --oneline` / `git status` for which roadmap steps are done.

## Summary

Build the logged-out landing page at `/` per the 5 PNGs — header, hero,
"Three steps, one list", planner tease, "Pick a plan", footer, plus a
mobile bottom nav — as static presentational markup. Replace
`SignInPlaceholder` with it. Add a public `/recipes` "coming soon" stub
that absorbs the browse/planner nav targets. Land the Crockpot colour
palette and the Fraunces + Newsreader fonts as design tokens. Every
call-to-action either fires the Google redirect, routes to `/recipes`,
or smooth-scrolls to a section on the same page.

## Decisions and why

### Palette — lands in `src/index.css`, `:root` only, no dark mode

Source of truth: **Claude Design's own palette output** for these
designs (the tool that generated them). Not eyedropped. Light mode only
— the reskin has zero dark screenshots, CFE-001 already stripped
`next-themes` from `sonner`, and a second palette doubles the work for
nothing. Revisit only if a dark design appears.

Base colours → token mapping (convert hex to OKLCH to match the file's
existing style):

| Token | Colour | Role |
| --- | --- | --- |
| `--background` | `#FBF7EF` warm cream | page background |
| `--foreground` | `#23201B` charcoal | body text |
| `--card` / `--popover` | `#FFFCF6` | cards, slightly lighter than bg |
| `--card-foreground` / `--popover-foreground` | `#23201B` | |
| `--primary` | `#3E5A33` sage green | buttons, focus ring |
| `--primary-foreground` | `#FBF7EF` | button label |
| `--secondary` / `--muted` | `#EAE4D6` tan | pill backgrounds, dividers |
| `--secondary-foreground` | `#23201B` | |
| `--muted-foreground` | `#6B6353` taupe | secondary text ("30 mins · serves 4", labels) |
| `--accent` / `--accent-foreground` | `#EAE4D6` / `#23201B` | leave neutral — do **not** make this orange |
| `--border` / `--input` | `#E0D4BB` | subtle tan hairlines |
| `--ring` | `#3E5A33` | green focus outline |
| `--destructive` / `--destructive-foreground` | keep current red | error toasts need real danger-red; `#C0552B` is not it |

Custom decorative tokens (also in `@theme inline` as `--color-*` so
they're usable as utilities later):

| Token | Colour | Role |
| --- | --- | --- |
| `--accent-rust` | `#C0552B` burnt orange | "Three steps" card 01 top border; decorative only, never an interactive colour |
| `--accent-gold` | `#E8C56B` soft gold | "Three steps" card 03 top border |
| card 02 top border | reuse `--primary` | |

The charcoal footer surface reuses `--foreground` as a background; no
separate token.

**Open item for the first visual check:** Claude Design's notes assign
the tan `#EAE4D6` to "page background" and cream `#FBF7EF` to "cards".
The screenshots read lighter than a tan, so the table above uses
cream-bg / lighter-card. If cards don't separate from the background on
the running page, swap `--background` to `#EAE4D6` and `--card` stays
`#FBF7EF`.

### Fonts — Google Fonts `<link>` in `index.html`, two serif tokens

`index.html` gets two `<link rel="preconnect">` (fonts.googleapis.com,
fonts.gstatic.com crossorigin) + one stylesheet `<link>` with
`display=swap`, both variable with optical sizing:

- **Fraunces** — `opsz 9..144`, `wght 400..600` — display headings
- **Newsreader** — `opsz 6..72`, `wght 400..500` — body and everything
  else

`src/index.css` `@theme`:

```
--font-display: "Fraunces", Georgia, "Times New Roman", serif;
--font-sans:    "Newsreader", Georgia, "Times New Roman", serif;
```

Keep the token name `--font-sans` even though it points at a serif —
it's Tailwind's default-text slot; renaming churns `font-sans` across
every file for no gain. Headings use a `font-display` utility. There is
**no UI-sans font** — the design is display-serif + body-serif only.

**Rejected:** self-hosting via `@fontsource-variable/*`. The correct
perf/privacy answer, but adds deps + import wiring for a pre-launch SPA
with no SSR and no perf budget; the old Next.js app used
`next/font/google`, same posture. Revisit at the Astro migration (Astro
has first-class font handling) or when perf becomes real.

### Page is landing-local, not the start of CFE-006's `AppShell`

CFE-002a parked "AppShell / nav / layout route / `<Outlet>`" for
CFE-006. The public marketing header (Recipes / How it works / Pricing /
Sign in) and the authenticated app nav are different components with
different contents. `src/features/landing/` owns its own header, footer,
and mobile bottom nav; CFE-006 builds the authed chrome separately.
Forcing one shared shell now is premature abstraction CFE-006 would have
to unpick.

### Mobile bottom nav is in scope

`loggedOutUserNav.png` shows a fixed bottom bar (magnifier "Browse
Recipes" + arrow "Login") on the mobile landing. Build it landing-local,
shown `< md`. Deferring it leaves the mobile pages visibly short of
`lp2.x`.

### `/recipes` stub is public

The landing links to `/recipes` from a logged-out state, so the stub
renders without `RequireAuth` — a plain centred "coming soon". Absorbs
the browse and planner nav targets until CFE-004. Catch-all `*` still
redirects to `/`.

### Logged-in `/` keeps redirecting to `/menu`

`AppRoutes` already sends an authenticated visitor from `/` to
`DEFAULT_AUTHENTICATED_ROUTE`. The marketing page is logged-out only
(the only state the screenshots show). Unchanged.

### Recipe cards are gray placeholders with generic captions

The hero's 3 overlapping rotated "polaroid" cards (1 on mobile) keep
their layout — white frame, shadow, few-degree rotation — but the photo
area is a flat gray block and the captions are generic ("Recipe name" /
"30 mins · serves 4"), **not** the screenshot's real recipe names, so
nobody reads them as a broken feature. Real cards land in CFE-004.

### CTA wiring

| Control | Action |
| --- | --- |
| "Sign in" (header), "Continue with Google" (hero), "Get started free" (Free card) | `window.location.href = ${import.meta.env.VITE_API_URL}/auth/google/login` |
| "Browse recipes" (hero), "Recipes" (nav + footer), mobile "Browse Recipes" | `<Link to="/recipes">` |
| "How it works", "Pricing" (nav), "Pricing" (footer), "See the planner" | same-page smooth-scroll anchor (`#how-it-works`, `#pricing`; "See the planner" → `#pricing`) |
| Premium / Pro "Coming soon" | disabled `<Button>`, no handler |
| "Privacy", "Contact" (footer) | inert link-styled text — no such pages yet |
| mobile "Login" (bottom nav) | Google redirect |

### Static content

"189 recipes · free forever tier · no card" (mobile: "189 recipes ·
free forever"), "Browse 189 recipes…", all pricing figures and feature
lists — hardcoded strings. No API call for a live recipe count.

### Other

- `SignInPlaceholder.tsx` is deleted; `AppRoutes` renders `<LandingPage />`
  at `/` for the logged-out branch.
- `App.test.tsx` updated: it currently asserts a single "continue with
  google" button, which the landing makes ambiguous. Reassert on the
  Fraunces headline text ("Never write a shopping list again.") instead.
- `index.html` `<title>` → `Crockpot — Never write a shopping list
  again`. Per-route `useDocumentTitle` stays deferred (CFE-002a
  non-goal).
- New shadcn components (`card`, `badge`, `separator`) generated via the
  shadcn CLI as needed — the `src/components/ui/**` oxlint carve-out
  already covers them.
- Icons from `lucide-react` (chef hat, arrow-right, search, log-in,
  check).
- Astro-migration constraint (master spec): the landing fetches nothing,
  so it's nearly free — use `<Link>` / `<a href>` not `useNavigate`,
  keep `LandingPage` a plain component, no `useParams` / `useLocation`.

## Acceptance criteria

- [ ] `src/index.css` — palette per the table (`:root` + `@theme
      inline`), `--font-display` / `--font-sans` set to Fraunces /
      Newsreader, `--accent-rust` / `--accent-gold` added.
- [ ] `index.html` — Google Fonts `<link>`s (preconnect + stylesheet,
      `display=swap`); `<title>` updated.
- [ ] `src/features/landing/LandingPage.tsx` — all 6 sections per the
      PNGs, rendered at `/` for the logged-out branch of `AppRoutes`.
- [ ] Header: logo, nav (Recipes / How it works / Pricing), "Sign in"
      pill. Mobile: logo + "Sign in" only.
- [ ] Hero: eyebrow pill, Fraunces headline, body copy, "Browse
      recipes" (filled), "OR SAVE YOUR MENU" divider, "Continue with
      Google" (outline). No "continue with email instead". 3 gray
      placeholder cards (1 on mobile).
- [ ] "Three steps, one list": 3 cards, rust / green / gold top
      borders, 01–03, Fraunces subheads.
- [ ] Planner tease: dashed callout, "OPTIONAL" badge, "See the
      planner" button.
- [ ] "Pick a plan": Free (£0, "START HERE" tab, "Get started free"
      filled), Premium (£4.99, "Coming soon" disabled), Pro (£9.99,
      "Coming soon" disabled).
- [ ] Footer: charcoal bar, logo, links (Recipes / Pricing / Privacy /
      Contact).
- [ ] Mobile bottom nav (`< md`): "Browse Recipes" + "Login" per
      `loggedOutUserNav.png`.
- [ ] All CTAs wired per the CTA table.
- [ ] `src/features/recipes/RecipesComingSoon.tsx` at `/recipes` —
      public, plain "coming soon". Catch-all `*` still → `/`.
- [ ] `SignInPlaceholder.tsx` deleted; no remaining imports.
- [ ] `src/app/App.test.tsx` — reasserted on the headline text.
- [ ] `npm test`, `npm run build`, `npm run lint`, `npm run
      format:check` all clean.
- [ ] Visual + interactive verification below passes.

## Non-goals

- Dark mode. No design, `sonner` has no theme wiring.
- A shared `AppShell` / authed nav / layout route — CFE-006.
- Real `/recipes`, `/pricing`, `/privacy`, `/contact` pages — later
  tickets; footer links to the last two are inert for now.
- `useDocumentTitle` / per-route titles — first real authed screen.
- Favicon swap to the chef-hat mark — flagged, not done here.
- Live recipe count from the API — `189` is hardcoded.
- Any email/password auth surface — CFE-002b.
- Unit tests for the page — presentational; see Verification.
- A full design-token / typography-scale system beyond what the landing
  needs — extend per-ticket as real screens arrive.

## Verification mode

**Visual — screenshot comparison, run by the founder.** Founder runs
`npm run dev` and compares the rendered page against the PNGs (project
`CLAUDE.md`: Claude does not start/drive the dev server). States:

- desktop hero + "Three steps" → `lp1.0.png`
- desktop planner tease + "Pick a plan" + footer → `lp1.1.png`
- mobile hero → `lp2.0.png`
- mobile planner tease + pricing + footer → `lp2.1.png`
- mobile bottom nav → `loggedOutUserNav.png`
- palette + fonts render; `--background` / `--card` separate acceptably
  (swap per the Palette open item if not)

**Interactive — hands-on by the founder.** `npm run dev` (+ local
`crockpot-go` for the redirect):

1. Logged out, visit `/` → full landing renders.
2. "Sign in" / hero "Continue with Google" / "Get started free" → each
   leaves to `crockpot-go`'s `/auth/google/login`.
3. "Browse recipes" / nav "Recipes" / mobile "Browse Recipes" → `/recipes`
   stub.
4. Nav "How it works" / "Pricing" / "See the planner" → smooth-scroll to
   the right section.
5. Premium / Pro "Coming soon" buttons are inert.
6. Log in via Google → land on `/menu`; then visit `/` → redirects to
   `/menu` (no landing).
7. Unknown path (e.g. `/nope`) → redirects to `/`.
8. Resize to mobile width → nav collapses to logo + "Sign in", hero
   shows 1 card, bottom nav appears.

**Logic — none.** The page is presentational; the `/` authed/loading/
unauthed branch already lives in `AppRoutes` and is exercised by
`App.test.tsx` (reasserted on the headline). No `LandingPage` render
test — interactive-only, founder's call at the grill.

**No live-verification dependency on `crockpot-go`** beyond the Google
redirect target, already covered by CFE-002a.

## Roadmap

Lower layers (tokens) first, then the page, then the wiring.

### 1. Palette + fonts — `src/index.css`, `index.html`

- Remap `:root` tokens and `@theme inline` `--color-*` per the Palette
  table; add `--accent-rust`, `--accent-gold`.
- Set `--font-display` / `--font-sans`.
- `index.html`: font `<link>`s + `<title>`.
- No visible change yet except the existing `/menu` + toast restyle —
  founder eyeballs that nothing regressed.

### 2. `/recipes` stub + route

- `src/features/recipes/RecipesComingSoon.tsx` — plain centred text.
- `AppRoutes`: add `<Route path="/recipes" …>` (public), keep catch-all.

### 3. `LandingPage` shell + header + footer

- `src/features/landing/LandingPage.tsx` rendered at `/` (logged-out
  branch), `SignInPlaceholder` deleted.
- Header + footer + mobile bottom nav, CTAs wired.
- `App.test.tsx` reasserted; `npm test` green.

### 4. Hero

- Eyebrow, headline, copy, buttons, divider, gray placeholder cards
  (responsive 3 → 1).

### 5. "Three steps" + planner tease

- 3 accent-bordered cards; dashed callout with "OPTIONAL" badge.

### 6. "Pick a plan" + polish pass

- 3 pricing cards; "START HERE" / "Coming soon" states.
- Full responsive pass against `lp2.x`; smooth-scroll anchors.
- `npm test` / `build` / `lint` / `format:check` green, then the visual
  + interactive verification. Commit.
